spring:
  application:
    name: hotswap-demo

server:
  port: 8080

# HotSwap is enabled by default — no config needed.
# All @HotSwap fields will auto-register and start watching.
hotswap:
  enabled: true

logging:
  level:
    com.hotswap: DEBUG
